#include <bits/stdc++.h>
using namespace std;
int main()
{
    int x = 30;
    int y = 20;

    int z = (x > y) ? x : y; // z will be equal to 20
    cout<<z;

    bool isStudent = true;
    string message = isStudent ? "You are a student." : "You are not a student."; // message will be equal to "You are a student."
}